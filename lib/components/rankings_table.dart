import 'package:flutter/material.dart';

class RankingsTable extends StatelessWidget {
  const RankingsTable({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}